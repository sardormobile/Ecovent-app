<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.example.ecovent">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoVent">
        <activity android:name=".screen.ValyutaCalculationActivity"></activity>
        <activity android:name=".utils.CalculatorActivity"></activity>
        <activity android:name=".DemoActivity" />
        <activity android:name=".Save.SaveActivity" />
        <activity android:name=".ItemActivity0" />
        <activity android:name=".CalculatActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>